[project]
name = "dtro-safety-health-check"
version = "1.0.0"
description = "DTRO 안전보건 체크리스트"
requires-python = ">=3.10"
dependencies = [
    "flet",
]

[tool.flet.app]
path = "."

[tool.flet.android]
min_sdk_version = 21